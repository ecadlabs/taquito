name: Node.js CI for Flextesa

on:
  push:
    branches:
      - 457-flextesa-kunal-2
jobs:
  integration-tests-flextesanet:
    runs-on: self-hosted
    strategy:
      matrix:
        node: [12.x]
    steps:
    #- uses: actions/checkout@v2
    #- uses: actions/setup-node@v1
    #  with:
    #    node-version: ${{ matrix.node }}
    #- uses: actions/cache@v1
    #  with:
    #    path: ~/.npm
    #    key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
    #    restore-keys: |
    #      ${{ runner.os }}-node-
    #- run: npm ci
    #- run: npm run lerna -- bootstrap
    #- run: npm run build
    - run: docker rm -f my-sandbox tezos-key-gen-api_redis_1 tezos-key-gen-api_signatory_1
    - run: rm -rf ~/.tezos_client/
    - run: docker run --rm --name my-sandbox --detach -p 8732:20000  tqtezos/flextesa:20210602 flobox start   
    - uses: actions/checkout@v2
      with:
        repository: kjaiswal/tezos-key-gen-api
        ref: 457-flextesa-support-kunal
    - uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node }}
    - run: docker-compose up
    - run: npm run build
    - run: npm run start
    #- run: cd integration-tests && npm run test:florencenet -- --maxWorkers=8
    #  env:
    #    CI: true
    #    TEZOS_RPC_FLORENCENET: ${{ secrets.TEZOS_RPC_FLORENCENET }}
