parameter unit ;
storage unit ;
view "loop" address never { CAR; DUP; VIEW "loop" never; ASSERT_SOME } ;
code { CDR ;
       SELF ;
       ADDRESS ;
       DUP ;
       VIEW "loop" never ;
       ASSERT_SOME ;
       DROP ;
       NIL operation ;
       PAIR } ;
