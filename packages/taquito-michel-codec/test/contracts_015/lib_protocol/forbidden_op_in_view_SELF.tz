# This contract uses SELF in a view, which is forbidden.
{
  storage unit ;
  parameter unit ;
  code {
         CAR ;
         NIL operation ;
         PAIR
  } ;
  view "v" unit unit { DROP ; SELF ; DROP ; UNIT }
}
