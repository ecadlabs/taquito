parameter unit;
storage unit;
code { DROP;
       UNIT;
       AMOUNT;
       NONE key_hash;
       CREATE_CONTRACT
         { parameter unit ;
           storage unit ;
           code
             { CDR;
               NIL operation;
               PAIR; } };
       DIP {DROP; PUSH unit Unit; NIL operation};CONS ; PAIR}
