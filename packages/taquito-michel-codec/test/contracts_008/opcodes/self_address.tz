parameter unit;
storage unit;
code {
       DROP;
       LAMBDA unit address { DROP; SELF_ADDRESS };
       UNIT;
       EXEC;
       SELF; ADDRESS;
       ASSERT_CMPEQ;
       UNIT; NIL operation; PAIR
     }
