# Taquito Sapling Spend Params

Internal data package for `@taquito/sapling`.

It contains the vendored Sapling spend proving parameters so the main `@taquito/sapling` package can stay below registry upload limits while remaining installable without a postinstall download step.
