# Native Git hooks (Git 2.54+). Included into local config via `git config include.path`.
# Wired up by the `prepare` script in package.json on `npm install`.
# Do not edit .git/config directly - edit this file.

[hook "precommit"]
	event = pre-commit
	command = npm run precommit
